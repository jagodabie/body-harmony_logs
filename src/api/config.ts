export const apiBaseUrl = process.env.REACT_APP_API_BASE_URL ?? '';

export const bodyMetricsUrl = `${apiBaseUrl}/body-metrics`;
export const mealsUrl = `${apiBaseUrl}/meals`;
export const productsUrl = `${apiBaseUrl}/products`;
